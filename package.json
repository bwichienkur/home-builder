{
  "name": "olsen-custom-homes",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "node server/index.js",
    "buildertrend:pull": "node -e \"import('./server/buildertrend/pull.js').then((m) => m.pullBuildertrend().then((p) => { console.log(JSON.stringify({ ok: true, authMethod: p.authMethod, pulledAt: p.pulledAt, statuses: p.statuses }, null, 2)); }).catch((e) => { console.error(e.message); process.exit(1); }))\"",
    "buildertrend:update-snapshot": "vitest run scripts/update-live-snapshot.spec.ts",
    "pipedrive:pull": "node -e \"import('./server/pipedrive/pull.js').then((m) => m.pullPipedrive().then((p) => { console.log(JSON.stringify({ ok: true, pulledAt: p.pulledAt, company: p.reports.company, openDeals: p.reports.openDeals.length, wonDeals: p.reports.wonDeals.length }, null, 2)); }).catch((e) => { console.error(e.message); process.exit(1); }))\"",
    "dashboard:update-snapshot": "vitest run scripts/update-live-snapshot.spec.ts",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@react-three/drei": "latest",
    "@react-three/fiber": "latest",
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "lucide-react": "latest",
    "pg": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "^7.18.2",
    "three": "latest",
    "xlsx": "^0.18.5",
    "zod": "latest",
    "zustand": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@types/three": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "5.9.3",
    "vite": "latest",
    "vitest": "latest"
  }
}
