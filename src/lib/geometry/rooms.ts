import * as THREE from 'three'; import type { Point,Wall } from '../../types';
const key=(p:Point)=>`${Math.round(p.x)},${Math.round(p.y)}`;
export function detectRoomPolygons(walls:Wall[]):Point[][]{const unused=new Set(walls.map(w=>w.id)),rooms:Point[][]=[];while(unused.size){const first=walls.find(w=>unused.has(w.id))!;unused.delete(first.id);const chain=[first.start,first.end];let cursor=first.end;for(let guard=0;guard<walls.length+1;guard++){const next=walls.find(w=>unused.has(w.id)&&(key(w.start)===key(cursor)||key(w.end)===key(cursor)));if(!next)break;unused.delete(next.id);cursor=key(next.start)===key(cursor)?next.end:next.start;if(key(cursor)===key(chain[0])){if(chain.length>=3)rooms.push(chain);break}chain.push(cursor)}}return rooms}
export function roomShape(points:Point[],origin={x:420,y:330},scale=80){const shape=new THREE.Shape();points.forEach((p,i)=>{const x=(p.x-origin.x)/scale,y=-(p.y-origin.y)/scale;i?shape.lineTo(x,y):shape.moveTo(x,y)});shape.closePath();return shape}
export function roomArea(points:Point[],scale=80){let area=0;for(let i=0;i<points.length;i++){const a=points[i],b=points[(i+1)%points.length];area+=a.x*b.y-b.x*a.y}return Math.abs(area/2)/(scale*scale)}
export function validatePlan(walls:Wall[]){const rooms=detectRoomPolygons(walls);const endpoints=new Map<string,number>();walls.forEach(w=>[w.start,w.end].forEach(p=>endpoints.set(key(p),(endpoints.get(key(p))??0)+1)));return{rooms,unclosed:[...endpoints].filter(([,count])=>count%2!==0).map(([point])=>point),valid:rooms.length>0}}
